paths:
    migrations: '%%PHINX_CONFIG_DIR%%/migrations'
    seeds: '%%PHINX_CONFIG_DIR%%//migrations/seeds'

environments:
    default_migration_table: phinxlog
    default_database: xxxxxxxxxxxx
    production:
        adapter: mysql
        host: localhost
        name: tatrydevelopment
        user: root
        pass: 'root'
        port: 3308
        charset: utf8mb4
        collation: utf8mb4_slovak_ci

    development:
        adapter: mysql
        host: localhost
        name: tatrydevelopment
        user: root
        pass: 'root'
        port: 3308
        charset: utf8mb4
        collation: utf8mb4_slovak_ci

    testing:
        adapter: mysql
        host: localhost
        name: tatrydevelopment
        user: root
        pass: 'root'
        port: 3308
        charset: utf8mb4
        collation: utf8mb4_slovak_ci

version_order: creation
